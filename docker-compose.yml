version: "3"
services:
  datasource-observer:
    build:
      context: .
    image: wacodis/datasource-observer:1.1.1
    ports:
      - 8080:8080
    environment: 
      - spring.rabbitmq.host=test.52north.org

